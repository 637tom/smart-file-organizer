name: file-organizer
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - google-genai
      - python-dotenv
      - pillow
      - pypdf
      - python-docx
      - requests
